/* ==========================================================================
   BoxnDots — Custom cursor (disabled)
   Currently using the normal system cursor. This stub is kept so index.html
   can reference it without a 404; re-enable by removing the early return.
   ========================================================================== */
(function () {
  return; // custom cursor disabled — using the normal system cursor
})();
