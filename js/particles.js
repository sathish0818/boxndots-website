/* ==========================================================================
   BoxnDots — Foreground particle layer (disabled)
   Kept as a stub so index.html can reference it without a 404.
   Re-enable by removing the early return and restoring the canvas logic.
   ========================================================================== */
(function () {
  return; // disabled — clean canvas, no 2D particle layer
})();
